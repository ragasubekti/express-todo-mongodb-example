# express-todo-mongodb-example

How to run?

- First run `yarn`
- Copy the `.env.example` to `.env` and setup your environtment variables, or run `yarn run setup`
- Run `yarn dev` for starting development server
